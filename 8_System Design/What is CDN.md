# What is a CDN?

A CDN (Content Delivery Network) caches content on edge servers close to users, so requests are served from nearby locations instead of always hitting the origin server.

It is most commonly used for static assets (images, CSS, JavaScript, videos), but with proper cache rules it can also cache some dynamic responses.
