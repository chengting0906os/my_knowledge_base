# SQL Injection

## 如何產生

- 本質：把「使用者輸入」直接拼接進 SQL 字串。
- 當輸入未正確驗證/轉義，攻擊者可改變原本查詢邏輯。

## 如何預防

- 一律使用 **參數化查詢**（prepared statement），不要字串拼接 SQL。
- 對輸入做 **白名單驗證**（型別、長度、格式）。
- 資料庫帳號採 **最小權限**（避免使用 root/all privileges）。

## 一句話

SQL Injection 的根因是「把不可信輸入當成 SQL 指令的一部分」；核心解法是「參數化查詢 + 最小權限 + 輸入驗證」。
